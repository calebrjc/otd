[project]
name = "otd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Caleb Johnson-Cantrell", email = "46861012+calebrjc@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.6",
    "requests>=2.32.3",
    "uvicorn[standard]>=0.34.0",
]

[project.scripts]
otd = "otd:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
